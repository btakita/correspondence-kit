# Contact metadata for correspondence-kit.
# Copy to contacts.toml and fill in your details.
# Labels are for lookup only — they tell Claude which conversation folders
# contain this person's threads. They do NOT affect sync routing.

[alex]
emails = ["alex@example.com", "alex@work.com"]
labels = ["correspondence"]
account = "personal"

# [dana]
# emails = ["dana@example.com"]
# labels = ["project-x"]
