# systemd user unit for corrkit watch
# Install: cp corrkit-watch.service ~/.config/systemd/user/
# Enable:  systemctl --user enable --now corrkit-watch
# Logs:    journalctl --user -u corrkit-watch -f

[Unit]
Description=corrkit IMAP polling daemon
After=network-online.target
Wants=network-online.target

[Service]
Type=simple
WorkingDirectory=%h/work/btakita/corrkit
ExecStart=/usr/bin/env uv run corrkit watch
Restart=on-failure
RestartSec=30

[Install]
WantedBy=default.target
